{
  "name": "timesheet",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "electron:win": "electron-packager ./ Timesheet --overwrite --platform=win32 --arch=ia32 --prune=true --out=release-builds --icon=./icons/icon",
    "electron:mac": "electron-packager ./ Timesheet --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds --icon=./icons/icon"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.6.1",
    "jsonfile-promised": "0.0.1",
    "moment": "^2.18.1"
  }
}
